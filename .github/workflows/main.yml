name: VPS Free Ubuntu by SIFO

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
    - name: Start VPS on Ubuntu ☁️
      run: |
        echo "✅ VPS Ubuntu started successfully!"
        sudo apt update -y
        sudo apt install -y openssh-server curl
        sudo systemctl enable ssh
        sudo systemctl start ssh
        echo "root:123456" | sudo chpasswd
        echo "💻 Username: root"
        echo "🔑 Password: 123456"
        echo "🌍 Connecting to Serveo..."
        ssh -o StrictHostKeyChecking=no -R 2222:localhost:22 serveo.net &
        echo "🌐 Waiting for Serveo connection..."
        sleep 8
        echo ""
        echo "✅ VPS is ready!"
        echo "---------------------------------------"
        echo "Use this command in Termux or Linux:"
        echo ""
        echo "ssh root@serveo.net -p 2222"
        echo "Password: 123456"
        echo "---------------------------------------"
        echo "🕐 VPS will stay online for ~60 minutes"
        echo ""
        sleep 3540
        echo "⏰ VPS session ended. Bye!"
